package main

import "fmt"

func main() {
	defer fmt.Println("world!")
	fmt.Println("hello ")

	fmt.Println("counting")
	for i := 0; i < 10; i++ {
		defer fmt.Println(i)
	}
	fmt.Println("done")
} //defer具有堆的特性，讲究后进先出，先进后出，因此会有推迟
