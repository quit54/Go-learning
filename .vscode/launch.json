{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug simple Go code",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}