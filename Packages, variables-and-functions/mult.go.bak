package main

import (
	"fmt"
)

func swap(x, y string) (string, string) {
	return y, x
}
func spilt(sum int) (x, y int) {
	x = sum + 1
	y = sum - 1
	return
}

func main() {
	a, b := swap("hello", "world")
	fmt.Println(a, b)

	fmt.Println(spilt(10))
}
