package main

import (
	"fmt"
	"math"
)

func main() {
	var x, y int32 = 3, 4
	var f float64 = math.Sqrt(float64(x*x + y*y))
	var z uint = uint(f)
	fmt.Println(x, y, z)

	i := 46.3
	j := i
	fmt.Printf("j is type of %T\n", j)
	fmt.Printf("j is type of %T\n", i)
}
