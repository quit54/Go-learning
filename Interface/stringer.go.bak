package main

import "fmt"

type Person struct {
	Name string
	Age  int
}

func (p Person) String() string {
	return fmt.Sprintf("%v (%v years)", p.Name, p.Age)
}
func main() {
	a := Person{"Arthur Dent", 42}
	z := Person{"Zaphod Beedlebrox", 9001}
	fmt.Println(a, z)
}

//stringer是一个可以用字符串描述自己的类型，fmt包都通过此接口来打印值
//fmt包在打印数值的时候，会检验该数值是否实现了stringer接口
//如果实现了stringer接口，fmt包会调用string（）方法来说去字符串的表示
/*
type Stringer interface {
    String() string
}这是这种类型接口的dinginess
*/
//切记go语言的哲学，接口是行为的定义，而不是类型的定义
